package org.example.rulebased.streaming.flinkdroolspattern;
unit FireDetectRuleUnit;

rule "fire detect"
    when
        $s: /sensorDatas[ temperature > 1000 ]
    then
        FireAlarm alarm = new FireAlarm($s.getDate(), $s.getTemperature());
        alarmList.add(alarm);
end
